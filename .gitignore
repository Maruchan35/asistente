# Environments
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/

# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*$py.class
*.pyo
*.pyd

# ── Config y archivos sensibles (API keys, tokens, credenciales) ──────────────
config/api_keys.json
config/rules.json
config/vision_guardian_state.json
config/whatsapp_contacts.json
# config/whatsapp_coords.json  ← se incluye en el repo (1920x1080, universal)
config/hand_landmarker.task
config/*.png
config/*.jpg
config/user_profile.json
config/user_profile.bak
config/google_credentials.json
config/google_token_*.json
config/.spotify_cache
config/vosk_model/
config/morning_brief_log.json
config/doc_index.json
config/factory_skills.json
config/autonomy.json
# Backups locales con datos sensibles — NUNCA subir
config/*.bak
config/api_keys.json.plain.bak
memory/whatsapp_log.jsonl
memory/*.jsonl
config/netflix_prefs.json
config/pad_notes.txt
custom_tools.json
memory/*.json
memory/*.bak
memory/sessions/
.spotify_cache

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log
jarvis.log
logs/
logs/*.jsonl
logs/*.jsonl.1

# ── Backups ───────────────────────────────────────────────────────────────────
backups/

# ── Scratch / testing artifacts ───────────────────────────────────────────────
patch_main.py
scratch/
tests/_out/
]

# ── IDE / OS files ────────────────────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# ── Claude Code session data ──────────────────────────────────────────────────
.claude/

# ── Audit/dev temporaries ─────────────────────────────────────────────────────
audit_skills.py

# Keyfile de cifrado (estable, local) y backups de api_keys — NUNCA subir
config/.jarvis_key
config/api_keys.json.enc.bak
config/api_keys.json.plain.bak
config/*.bak
